{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": "src",             // Thêm dòng này
    "paths": {
      "@/*": ["*"]               // Thêm dòng này
    }
  }
}
